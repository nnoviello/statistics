package org.grayleaves.problem;

public interface Update
{
	/**
	 * Marker interface for all classes to be created by @link{JsonUpdate} from a user interface that submits its updates in JSON format. 
	 */
}
